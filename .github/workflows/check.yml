name: Check

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    name: Backend tests and build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.14'
      - name: Install locked dependencies
        if: github.event_name != 'pull_request' || github.event.pull_request.user.login != 'dependabot[bot]'
        run: bun install --frozen-lockfile
      - name: Install Dependabot dependencies
        if: github.event_name == 'pull_request' && github.event.pull_request.user.login == 'dependabot[bot]'
        run: bun install --no-save
      - run: bun run test:coverage
      - run: bun run build
      # Placeholders only validate that the compose file parses. Real deployments must supply
      # generated secrets: compose fails when either variable is unset, which is the point.
      - name: Validate compose file
        env:
          APP_MASTER_KEY: ci-placeholder-not-a-real-key
          DEVICE_ASSET_SIGNING_KEY: ci-placeholder-not-a-real-key
        run: docker compose config --quiet
